spring:
  config:
    import: "configserver:"
  cloud:
    bus:
      enabled: false
      destination: bus
    vault:
      enabled: false
  application:
    name: test-app

jme:
  test:
    application: "somevalue"
