jeap:
  monitor:
    prometheus:
      user: prometheus
      password: "{noop}thisisthepasswordusedtoaccesstheprometheusendpointsforthejeapmonitoringstarter"
    actuator:
      user: actuator
      password: "{noop}test"
spring:
  application:
    name: jme-monitor-test
server:
  servlet:
    context-path: "/${spring.application.name}"
management:
  server:
    port: 0
    base-path: "/jme-management-test"
