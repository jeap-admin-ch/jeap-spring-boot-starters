package ch.admin.bit.jeap.postgresql.aws.config;

import java.util.Properties;

/**
 * Properties set on this class will be passed directly to target data source during the configuration of a
 * HikariDataSource in {@link HikariDataSourceFactory#create}
 */
public class WrapperTargetDataSourceProperties extends Properties {
}
